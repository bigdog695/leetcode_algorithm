这里附上169的代码。
#Author:UncleDoggy
#Time:O(n)
#Space:O(1)
#Method:moore voting
#Code:
1.python version:
    def f(nums):
        major,cnt=0,0
        for i in nums:
            if cnt==0:
                major=i
                cnt=1
            else:
                if major!=i:
                    cnt-=1
                else:
                    cnt+=1
        return major
        
2.cpp version:to be continued
